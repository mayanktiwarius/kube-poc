apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxchaosdeployment.fullname" . }}-test-connection"
  labels:
    {{- include "nginxchaosdeployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxchaosdeployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
