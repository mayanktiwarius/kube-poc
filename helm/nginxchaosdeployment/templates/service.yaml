apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginxchaosdeployment.fullname" . }}
  labels:
    {{- include "nginxchaosdeployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginxchaosdeployment.selectorLabels" . | nindent 4 }}
