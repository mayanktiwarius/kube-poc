apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxstateful.fullname" . }}-test-connection"
  labels:
    {{- include "nginxstateful.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxstateful.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
